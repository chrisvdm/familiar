{
  "executor_id": "provider_a",
  "base_url": "http://localhost:8787",
  "token_env": "TEXTY_EXECUTOR_TOKEN",
  "tools": [
    {
      "tool_name": "notes.echo",
      "description": "Return a note back to the user",
      "input_schema": {
        "type": "object",
        "properties": {
          "note": {
            "type": "string"
          }
        },
        "required": ["note"]
      }
    }
  ]
}
