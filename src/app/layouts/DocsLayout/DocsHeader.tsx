import { FamiliarName } from "@/app/components/familiar-name"

const DocsHeader = () => {
    return(
        <header>
            <a href="/" title="Back to home page"><FamiliarName/></a>
        </header>
    )
}

export default DocsHeader