# 2026-03-24 API Reference Page

- added a dedicated API Reference page to the native `/docs` section
- pulled the content from the existing API spec and quickstart docs, but rewrote it in a simpler format
- added endpoint-by-endpoint examples, common status codes, and note callouts for important side details
