# 2026-03-24 familiar Branding Pass

- renamed the current user-facing product references from `Texty` to `familiar`
- updated the main README, active architecture and API docs, example READMEs, and demo page copy
- updated the web UI wordmarks and chat labels to show `familiar`
- left technical contract names such as `texty.json`, existing API paths, and the current deployed worker slug unchanged to avoid turning the branding pass into a breaking runtime rename
