# 2026-03-24 Concepts Ordered By Importance

- reordered the Concepts page so the highest-signal concepts appear first
- moved `Executor` and `Integration` near the top
- moved lower-level supporting terms like `User` and `Account` later in the page
