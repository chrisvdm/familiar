# 2026-03-24 Italicize familiar In User Docs

- italicized the product name across the native `/docs` content
- updated the docs page label so `familiar docs` follows the same treatment
- added emphasis support to the native markdown renderer so italic markdown actually renders
