# 2026-03-24 API Reference Nav Titles

- changed the API Reference section headings from raw endpoint paths to action-based titles
- moved the endpoint paths into the body of each section so the sidebar is easier to scan
- kept the endpoint strings visible in the main content where readers still need the exact route
